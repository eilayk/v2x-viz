# v2x

This Rust library provides V2X encoding and decoding helpers used by both the simulation and visualizer crates.

## Current scope

Only ETSI CAM (Cooperative Awareness Message) messages are supported right now. The [c_its_parser](https://docs.rs/c-its-parser/latest/c_its_parser/) is used under the hood.
